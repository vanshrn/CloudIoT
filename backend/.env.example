# Copy to .env and fill in for local `cdk deploy` / `cdk synth`.
# These are consumed by your shell / CDK CLI, not bundled into Lambda code.

CDK_DEFAULT_ACCOUNT=123456789012
CDK_DEFAULT_REGION=us-east-1

# Passed as CDK context: -c stage=dev|staging|prod (defaults to dev)
STAGE=dev
